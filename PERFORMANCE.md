# 随机播放启动性能

2026-09-16：待播音频预取目标从 1 首增加至 2 首，下载并发仍为 1。下面的实测数据保留其原始版本条件。

## 个人非商业原型复测 · 2026-09-15

已启用逐曲许可证筛选、单首预取与启动缓存清理。在本机使用真实 Jamendo 接口、仅复用数字编号边界的一次测量中，从后端进程启动到首首歌曲信息约 **0.94 秒**，地区结果约 **1.84 秒**，前 64KiB 音频约 **3.32 秒**。这是单次网络样本，不等同于实际出声时间，也不是设备性能保证。

## 历史版本 · 2026-09-14

> 以下实测记录的是 2026-09-14 的版本。个人非商业原型更新增加了逐曲许可证筛选，将音频预取从 4 首减为 1 首，并取消跨启动恢复音频；保留随机编号查询和地区批量请求。下列数字不能直接作为更新版本的速度保证。

## 改动

- 用在线最大歌曲编号确定抽样边界，随机生成精确 ID 后批量查询，移除深分页。API 支持多个 ID 和 `id_desc` 排序，见 [Jamendo tracks](https://developer.jamendo.com/v3.0/tracks) 与[多值参数说明](https://developer.jamendo.com/v3.0/read-methods)。
- 以随机编号生成顺序选取成功结果，忽略未请求的编号、已播放编号和无音频结果；空缺编号重新抽取，不选择相邻编号。抽样对象仍是在线歌曲。
- `catalog-bounds.json` 只保存最大编号与时间，退出保留；6 小时后后台更新，最多容忍 30 天的旧边界。新上传歌曲可能要等边界刷新后进入抽样范围。
- 每批成功结果立即启动音频和地区预取，播放器无需等整个候选缓冲补满、整首音频下载或地区解析结束。
- 地区请求按艺人编号合并并去重。官方空结果直接交给 MusicBrainz，遵守原有每秒一次的请求间隔；临时失败不记作“无资料”。
- 当前音频流启动时取消相同歌曲的后台下载，把带宽留给播放和后续候选。

## 实测

同一台 Windows 电脑与网络，使用真实 Jamendo 接口。每次启动全新的临时数据目录，歌曲、音频、地区都没有缓存；标为“已初始化范围”的运行只复用最大编号与查询时间。下面都是从启动后端进程开始计时，单位为秒。

| 场景 | 首首歌曲信息 | 地区结果 | 前 64KiB 音频 |
| --- | ---: | ---: | ---: |
| 改动前，随机深分页 | 29.06 | 29.06 | 30.37 |
| 改动后，首次查询编号范围 | 18.40 | 19.32 | 20.05 |
| 改动后，已初始化范围，第 1 次 | 3.93 | 4.52 | 5.35 |
| 改动后，已初始化范围，第 2 次 | 1.58 | 2.22 | 3.52 |
| 改动后，已初始化范围，第 3 次 | 1.09 | 1.76 | 2.94 |

分段日志显示，旧深分页单次约 20–22 秒；新精确 ID 查询样本约 0.35–0.50 秒，首次范围初始化样本约 17.52 秒。请求、TLS 连接、歌曲音频和网络波动仍影响总耗时，以上少量样本不是稳定性保证。

“前 64KiB 音频”仅是数据可用指标，不等同于浏览器解码后实际出声时间。上述地区样本都有官方可用资料；依赖 MusicBrainz 或无可信资料的歌曲仍可能更慢或保持未知。

## 复测

```powershell
# 全新临时缓存；从环境变量或本地配置读取 client_id，但不打印凭证
node web/scripts/measure-startup.cjs

# 只复用本地编号边界，歌曲和音频仍从网络随机获取
node web/scripts/measure-startup.cjs --reuse-bounds

# 显示不含凭证的请求分段耗时，并为本机保存查到的编号边界
node web/scripts/measure-startup.cjs --trace --prime-bounds

node --test test/startup-latency.test.js test/random-discovery.test.js
```

临时后端在测量后退出并删除自己的临时目录。测量脚本不会改动个人凭证或现有播放缓存。
