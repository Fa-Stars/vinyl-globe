# 世界唱片机 · 产品目标与原型需求

## 一句话目标

把当前的 World Vinyl 网页项目实体化为一台小型、可独立运行的桌面唱片机：联网随机播放来自世界各地的音乐，用屏幕显示专辑封面和像素地球，并通过机身上的实体按钮操作。

## 产品定位

- 这是一个桌面小型设备，不是“另一台电脑上打开网页”。
- 核心计算、网络、网页、音频播放和硬件控制都由一块单板机完成。
- 通过 3D 打印外壳、屏幕、扬声器、按钮和电源组合成完整产品。
- 设备应有唱片机的视觉和操作感觉，同时保留世界地图与音乐探索主题。

## 当前确定的硬件方向

- 主板：Orange Pi 3B 4GB。
- 系统：Debian/Ubuntu Desktop，运行 Node.js 服务和 Chromium 全屏界面。
- 屏幕：优先 7 英寸横向屏幕，推荐 1280×800；原型阶段优先 HDMI，后续再评估 MIPI DSI。
- 音频：原型阶段使用 3.5mm 有源音箱或 3.5mm 音频输出连接小型 D 类功放；后续可集成两个小型扬声器和独立音量旋钮。
- 控制：至少两个实体按钮：播放/暂停、下一首；后续可增加音量、上一首、静音和灯光控制。
- 存储：64GB 高耐久 MicroSD；稳定后可考虑 eMMC 或 NVMe。
- 散热：低矮散热片，必要时增加低噪声 5V 风扇。
- 网络：使用板载 Wi-Fi，必要时通过网线进行初始配置或稳定联网。
- 外壳：3D 打印小型桌面外壳，主板安装在屏幕后方，扬声器位于屏幕两侧或下方。

## 目标尺寸

推荐第一版外壳尺寸约为：

```text
宽 300 mm × 高 170 mm × 深 100 mm
```

可接受范围约为：

- 宽：280–320 mm
- 高：140–180 mm
- 深：80–110 mm

目标是可以放在普通书桌上、能够单手搬动的小型唱片机。若改用 5 英寸屏幕，可以进一步缩小到约 240×140×80 mm，但需要为网页增加小屏布局。

## 现有软件能力

- Node.js 后端，零第三方依赖，要求 Node.js ≥18。
- Chromium 显示网页界面。
- 随机播放歌曲并自动连播。
- 专辑封面显示在唱片动画上。
- 像素地球显示歌曲对应国家或地区。
- `R` 快捷键切换下一首。
- `Space` 快捷键播放/暂停。
- `?demo=1` 用于启动演示。
- Jamendo 提供完整歌曲；没有 Jamendo key 时提示配置，不接入试听兜底。
- 展示每首歌的作者、原页面及 CC 许可证；仅将授权信息可识别的歌曲加入候选。
- 歌曲池仅存在当前进程内存；`data/` 保存有期限的必要音频缓冲和艺术家地区资料。

## 硬件控制方向

第一版可以通过 GPIO 读取实体按钮，并映射到现有的 `R` 和 `Space` 行为。

更稳定的正式方案是增加本地硬件控制接口，例如：

```text
GPIO 按钮 → 本地控制接口 /api/control → 网页执行播放、暂停、切歌
```

这样不依赖 Chromium 当前是否拥有键盘焦点，也便于以后增加音量、上一首和静音功能。

## 原型阶段的最小采购清单

- Orange Pi 3B 4GB
- 64GB 高耐久 MicroSD 卡和读卡器
- 5V/3A USB-C 电源适配器及电源线
- 7 英寸横向 HDMI 触摸屏
- HDMI 视频线和 USB 触摸线
- 3.5mm 有源音箱，或小型功放加两个扬声器
- 两个瞬时按钮
- 一个旋转编码器（可选）
- 40 针 GPIO 转接板和杜邦线
- 低矮散热片
- M2.5 铜柱、螺丝和 3D 打印外壳材料
- USB 键盘、鼠标和网线（仅用于安装调试）

## 开发顺序

1. 在 Orange Pi 3B 上运行现有项目，确认网络、歌曲、封面和音频正常。
2. 接入 HDMI 屏幕，以 7 英寸横向尺寸调整网页布局。
3. 接入 3.5mm 音箱，完成连续播放和音量测试。
4. 接入播放/暂停和下一首实体按钮。
5. 配置 Node.js 和 Chromium 开机自启、全屏显示。
6. 设计并打印外壳，安排屏幕、扬声器、按钮、散热和电源接口。
7. 增加音量旋钮、静音、优雅关机和网络断开提示。
8. 在弱网下优化必要的短期播放缓冲；离线演示如有需要，应另使用自制或明确获许可的测试音频，不扩展 Jamendo API 曲库为离线模式。

## 设计原则

- 优先做出可操作的实体样机，再逐步集成内部结构。
- 第一版优先 HDMI 屏、3.5mm 音频和简单 GPIO 按钮，降低调试复杂度。
- 外壳尺寸和操作体验优先于堆叠复杂硬件。
- 不把产品方向退化为“电脑接屏幕”；所有核心组件应被整合进唱片机外壳。
- 面向长期运行时，要考虑散热、断电保护、缓存容量和网络中断。

## 仍待确定的事项

- 具体屏幕型号和横向安装方式。
- 扬声器尺寸、功放型号和音量旋钮方案。
- GPIO 引脚分配及按钮控制程序。
- 外壳正面布局和 3D 打印结构。
- 是否需要触摸操作，还是完全依赖实体按钮。
- 是否需要离线歌曲模式。
